from emulator import Emulator

if __name__ == '__main__':
    #TODO: parse arguments to get file name and options
    refresh_rate = 60
    scale = 4
    emulator = Emulator(refresh_rate=refresh_rate, scale=scale)